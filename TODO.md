# TODO - Mobile phones section update

- [ ] Add multiple smartphone products (Samsung, Vivo, OPPO, Realme, iPhone) to `src/data/mockData.ts` within `mockProducts`.
- [ ] Ensure each new product has: id, name (with model name), description, price (INR), category `Electronics`, stock, images, rating, reviewCount, and `featured` flag.
- [ ] (If needed) Adjust `mockReviews` for the new products to influence Best Sellers (based on reviewCount).
- [ ] Build the project (`npm run build`) to verify no TypeScript errors.
- [ ] Provide local dev URL/command to view the changes.

